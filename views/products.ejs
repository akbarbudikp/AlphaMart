<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Produk - AlphaMart</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --color-red: #D8251E;
            --color-blue: #0053A0;
            --color-yellow: #FDCB00;
            --text-dark: #333;
            --text-light: #fff;
            --bg-light: #f8f9fa;
            --border-color: #eee;
        }

        body {
            margin: 0;
            font-family: 'Poppins', sans-serif;
            background-color: var(--bg-light);
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 30px 20px;
        }

        .page-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 20px;
            flex-wrap: wrap;
        }

        .page-header h1 {
            color: var(--color-blue);
            margin: 0;
        }

        .search-form {
            display: flex;
        }

        .search-form input {
            padding: 10px;
            border: 1px solid #ccc;
            border-radius: 5px 0 0 5px;
            min-width: 250px;
        }

        .search-form button {
            padding: 10px 15px;
            border: none;
            background-color: var(--color-blue);
            color: white;
            border-radius: 0 5px 5px 0;
            cursor: pointer;
        }

        .btn {
            padding: 10px 20px;
            border-radius: 5px;
            text-decoration: none;
            font-weight: 600;
            transition: all 0.3s ease;
            cursor: pointer;
            border: none;
        }

        .btn-primary {
            background-color: var(--color-blue);
            color: var(--text-light);
        }

        .btn-primary:hover {
            background-color: #00417e;
        }

        .product-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
            gap: 25px;
        }

        .product-card {
            background-color: #fff;
            border: 1px solid var(--border-color);
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.05);
            display: flex;
            flex-direction: column;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .product-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.1);
        }

        .product-card img {
            width: 100%;
            height: 180px;
            object-fit: cover;
            border-radius: 10px 10px 0 0;
        }

        .product-info {
            padding: 20px;
            flex-grow: 1;
            display: flex;
            flex-direction: column;
        }

        .product-info h3 {
            margin: 0 0 10px 0;
            font-size: 1.1rem;
        }

        .product-info .stock {
            font-size: 0.9rem;
            color: #666;
            margin-bottom: 10px;
        }

        .product-info .price {
            font-size: 1.3rem;
            font-weight: 700;
            color: var(--color-red);
            margin-top: auto;
            margin-bottom: 15px;
        }

        .product-actions {
            padding: 0 20px 20px 20px;
        }

        .btn-add-cart {
            width: 100%;
            background-color: var(--color-red);
            color: var(--text-light);
        }

        .btn-add-cart:hover {
            background-color: #b01e18;
        }

        .admin-controls {
            margin-top: 15px;
            text-align: center;
            font-size: 0.9rem;
        }

        .admin-controls a {
            color: var(--color-blue);
            text-decoration: none;
            font-weight: 600;
        }

        .admin-controls a:hover {
            text-decoration: underline;
        }

        .notification-success {
            padding: 15px;
            margin-bottom: 20px;
            border-radius: 5px;
            background-color: #d4edda;
            color: #155724;
            border: 1px solid #c3e6cb;
        }

        .notification-error {
            padding: 15px;
            margin-bottom: 20px;
            border-radius: 5px;
            background-color: #f8d7da;
            color: #721c24;
            border: 1px solid #f5c6cb;
        }
    </style>
</head>

<body>
    <%- include('./partials/navbar.ejs') %>

        <main class="container">

            <% if (success && success.length> 0) { %>
                <div class="notification-success">
                    <%= success[0] %>
                </div>
                <% } %>

                    <% if (error && error.length> 0) { %>
                        <div class="notification-error">
                            <%= error[0] %>
                        </div>
                        <% } %>

                            <div class="page-header">
                                <h1>Daftar Produk</h1>
                                <form action="/products" method="GET" class="search-form">
                                    <input type="text" name="search" placeholder="Cari produk...">
                                    <button type="submit">Cari</button>
                                </form>
                            </div>

                            <% if (userRole==='Admin' ) { %>
                                <a href="/products/add" class="btn btn-primary" style="margin-bottom: 20px;">+ Tambah
                                    Produk Baru</a>
                                <% } %>

                                    <div class="product-grid">
                                        <% products.forEach(product=> { %>
                                            <div class="product-card">
                                                <img src="<%= product.imgUrl %>" alt="<%= product.name %>">
                                                <div class="product-info">
                                                    <h3>
                                                        <%= product.name %>
                                                    </h3>
                                                    <p class="stock">Stok: <%= product.stock %>
                                                    </p>
                                                    <p class="price">
                                                        <%= product.formattedPrice %>
                                                    </p>
                                                </div>
                                                <div class="product-actions">
                                                    <% if (product.stock> 0) { %>
                                                        <form action="/cart/add/<%= product.id %>" method="POST">
                                                            <button type="submit" class="btn btn-add-cart">Tambah ke
                                                                Keranjang</button>
                                                        </form>
                                                        <% } else { %>
                                                            <button class="btn btn-add-cart" disabled
                                                                style="background-color: grey;">Stok Habis</button>
                                                            <% } %>

                                                                <% if (userRole==='Admin' ) { %>
                                                                    <div class="admin-controls">
                                                                        <a
                                                                            href="/products/edit/<%= product.id %>">Edit</a>
                                                                        |
                                                                        <a href="/products/delete/<%= product.id %>"
                                                                            onclick="return confirm('Apakah Anda yakin ingin menghapus produk ini?')">Hapus</a>
                                                                    </div>
                                                                    <% } %>
                                                </div>
                                            </div>
                                            <% }) %>
                                    </div>

        </main>

</body>

</html>